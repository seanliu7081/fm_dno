# Learned heading enters observation features; the source remains IID.
defaults:
  - train_flowpolicy_heading_condition
  - _self_

name: train_flowpolicy_heading_condition_starvla_dit
policy:
  backbone_type: starvla_dit
  backbone_kwargs:
    num_register_tokens: 32
