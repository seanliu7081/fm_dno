# Optional action backbone with the existing observation-conditioned flow objective.
defaults:
  - train_flowpolicy
  - _self_

name: train_flowpolicy_starvla_dit
policy:
  backbone_type: starvla_dit
  backbone_kwargs:
    num_register_tokens: 32
